[tool.poetry]
name = "mazes"
version = "0.1.0"
description = "Simple maze generator in Python"
authors = ["bambi"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pillow = "^10.4.0"
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
